import {createTheme} from "@mui/material/styles";

export const customTheme = createTheme({
    palette: {
        primary: {
            main: '#6df',
        },
        secondary: {
            main: '#333',
        },
        inherit: {
            main: '#333',
        }
    },
});